include /universalHeader

doctype html
head
  link(rel='stylesheet' type='text/css' href='../public/styles.css')
body
+universalHeader
#editTopicsContent.content
  h1
    |Your Topics:
  each topic in data.topicList
    .topicRow
      h2
        |#{topic}
      a(href='/delete/topic/' + topic class="deleteLink")
        |Delete Topic
