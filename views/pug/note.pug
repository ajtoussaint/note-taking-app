include /universalHeader

doctype html
head
  link(rel='stylesheet' type='text/css' href='../public/styles.css')
body
+universalHeader
#noteContent.content
  h1
    |#{data.title}
  h3
    |Created on: #{data.dateCreated}
  h3
    |Updated on: #{data.dateUpdated}
  #noteMainContent
    !=content
  h2 Tags:
  #noteTags
    each tag in data.tags
      .noteTag
        |#{tag}
  a(href='/edit/' + data.title)
    |Edit Note
